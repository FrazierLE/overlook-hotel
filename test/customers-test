import { expect } from 'chai'
import CustomersRepository from '../src/classes/customersrepository'
import Customer from '../src/classes/customer'
import { sampleCustomers } from '../data/sample-data'

describe('CustomersRepository', () => {
  sampleCustomers
  let customer, customers

  beforeEach(() => {
    sampleCustomers
    customer = new Customer(sampleCustomers[0])
    customers = new CustomersRepository(sampleCustomers)
  })
  it('should be a function', () => {
    expect(CustomersRepository).to.be.a('function')
  })
})